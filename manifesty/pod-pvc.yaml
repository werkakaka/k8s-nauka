apiVersion: v1
kind: Pod
metadata:
  name: test-pvc
spec:
  containers:
    - name: nginx
      image: nginx
      volumeMounts:
        - name: moje-dane
          mountPath: /usr/share/nginx/html
  volumes:
    - name: moje-dane
      persistentVolumeClaim:
        claimName: moje-trwale-dane
