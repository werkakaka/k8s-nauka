apiVersion: v1
kind: Pod
metadata:
  name: test-emptydir
spec:
  containers:
    - name: nginx
      image: nginx
      volumeMounts:
        - name: moje-dane
          mountPath: /usr/share/nginx/html
  volumes:
    - name: moje-dane
      emptyDir: {}
